import CartPage from 'src/pages/CartPage/CartPage';

export const metadata = {
  title: 'Корзина',
  description: 'Корзина товаров',
};

const Cart = () => {
  return <CartPage />;
};

export default Cart;
